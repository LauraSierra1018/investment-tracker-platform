'use client';import {useState} from 'react';import {Sidebar,type Tab} from '@/components/sidebar';import {Dashboard} from '@/components/dashboard';import {Research} from '@/components/research';import {Watchlist} from '@/components/watchlist';import {Portfolio} from '@/components/portfolio';import {Criteria} from '@/components/criteria';
export default function Page(){const [tab,setTab]=useState<Tab>('dashboard');return <main className="min-h-screen"><div className="mx-auto flex max-w-[1600px]"><Sidebar tab={tab} setTab={setTab}/><section className="min-w-0 flex-1 p-5 md:p-8"><div className="mb-5 flex gap-2 overflow-auto lg:hidden">{(['dashboard','research','watchlist','portfolio','criteria'] as Tab[]).map(x=><button onClick={()=>setTab(x)} className={`rounded-xl px-4 py-2 text-sm font-bold ${tab===x?'bg-slate-950 text-white':'bg-white'}`} key={x}>{x}</button>)}</div>{tab==='dashboard'&&<Dashboard go={()=>setTab('research')}/>} {tab==='research'&&<Research/>}{tab==='watchlist'&&<Watchlist/>}{tab==='portfolio'&&<Portfolio/>}{tab==='criteria'&&<Criteria/>}</section></div></main>}
